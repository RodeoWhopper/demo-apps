# Kütüphane Plus – all values optional; defaults shown. Demo credentials only.
PORT=8017
# H2 file prefix (no extension). Docker image default: /app/data/library
DB_PATH=./data/library
# "prod" disables stack traces in error pages and the H2 console (already off by default)
SPRING_PROFILES_ACTIVE=prod
