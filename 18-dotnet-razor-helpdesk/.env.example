# Masaüstü Destek – all values optional; defaults shown. Demo credentials only.
ASPNETCORE_URLS=http://+:8018
ASPNETCORE_ENVIRONMENT=Production
# SQLite connection string (directory is created automatically). Docker default: /app/data/helpdesk.db
ConnectionStrings__Default=Data Source=data/helpdesk.db
