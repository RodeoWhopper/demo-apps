# Bakkal Panel - copy to .env for local runs
# demo value, not a real secret
APP_KEY=change-me-demo-key
# development enables on-screen stack traces; anything else hides them
APP_ENV=development
# absolute or relative path to the SQLite file (created + seeded automatically)
DB_PATH=data/bakkal.sqlite
