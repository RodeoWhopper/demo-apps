# docker-compose.yml reads these (all optional; defaults are demo-safe).
JWT_SECRET=change-me-demo-orbit-jwt-secret
ACCESS_TOKEN_MINUTES=60
CORS_ORIGINS=http://localhost:5015,http://127.0.0.1:5015
# Per-service env files: api/.env.example and web/.env.example
